# AGENTS.md

本文件适用于整个仓库，供后续开发者和编码代理在修改 LabPass 时遵循。

## 项目概览

LabPass 是面向南京邮电大学实验室安全教育系统的 Python 3.13+ 工具。项目支持 CAS 自动登录、课程刷课、课程答题和准入考试，通过交互式菜单操作。

项目版本以 `pyproject.toml` 中的 `[project].version` 为唯一来源。

## 目录与职责

- `app.py`：主程序，包含交互式菜单、登录认证、课程处理、答题提交、考试逻辑和证书下载。所有业务逻辑集中在此文件。
- `app.spec`：Windows 控制台 EXE 的 PyInstaller 配置。
- `labpass/`：核心库包。
  - `config.py`：URL、超时、请求头、应用 ID 等静态配置。
  - `crypto.py`：与学校前端兼容的 AES-CBC 密码加密。
  - `exceptions.py`：领域异常定义。
  - `__init__.py`：版本信息。
- `pyproject.toml`：项目元数据、依赖和工具配置。
- `uv.lock`：可复现依赖锁文件。
- `favicon.ico`：EXE 图标。

## 不可破坏的行为约束

### 登录与敏感信息

- 密码必须通过 `getpass` 隐藏输入，不得使用普通 `input`。
- 不得把账号密码、Token、Cookie、CAS ticket 写入控制台、异常消息或文件。
- Token 仅保存在内存中，程序退出即失效。
- 程序不持久化任何认证信息，不创建日志文件。

### 网络与超时

- 所有请求必须设置连接和读取超时。
- Token 过期时自动重新登录，不需要用户干预。

### 功能模式

- 模式 1（刷课+答题）：完成所有课程学习和答题，已完成的自动跳过。
- 模式 2（考试）：检查承诺书状态后参加考试，通过后下载证书。
- 模式 3（全流程）：依次执行模式 1 和 2。
- 执行完任意模式后返回菜单，不直接退出。

## 代码规范

- 目标运行时为 Python 3.13+，使用现代类型注解。
- 捕获最具体的异常；仅可在顶层设置兜底异常边界。
- 用户可恢复的错误应给出简洁中文消息。
- 不使用裸 `except`，不静默吞掉业务错误。

## 构建

Windows EXE 构建命令：

```powershell
uv run --group build pyinstaller --clean app.spec
```

构建后验证：

```powershell
.\dist\labpass-tool.exe --help
```

`app.spec` 必须保持 `console=True`，否则隐藏输入、进度输出和菜单交互无法可靠工作。

## 隐私要求

- EXE 不得包含任何打包者或测试者的真实个人信息（学号、姓名、密码等）。
- 帮助文本中的学号示例必须使用占位符（如 `B26XXXXXX`）。
- 分发时只发 `dist/labpass-tool.exe` 单文件，不要连同开发目录一起分发。

## README 同步

当以下内容变化时，必须同步更新 `README.md`：

- 交互式菜单、模式或操作流程；
- 登录方式、网络要求；
- 项目目录结构；
- 构建命令；
- Python 最低版本或项目版本。
